# AVM Audit Preparation Claude Commands

Install these in your `~/.claude/commands/` and invoke with `/<command>` from within Claude Code.

This repo gitignores all markdown and sh files that do not start with `avm`.
